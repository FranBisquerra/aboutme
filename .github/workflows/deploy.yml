name: Deploy

on:
  pull_request:
    types: [ closed ]
    branches:
      - develop
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to production
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.merged == true &&
       contains(github.event.pull_request.labels.*.name, 'autodeploy'))
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            source "$HOME/.sdkman/bin/sdkman-init.sh"
            export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh"
            nvm use 24 || nvm install 24
          
            # 4. Verificación en los logs de GitHub Actions
            echo "=== Verificando versiones en el entorno SSH ==="
            node -v
            npm -v
            
            cd ~/Projects/aboutme
            git pull origin develop
            ./gradlew dockerRun -Pprofile=pro -x test
