spring:
  datasource:
    url: ${DB_URL:jdbc:mariadb://localhost:3306/aboutme}
    username: ${DB_USERNAME:user}
    password: ${DB_PASSWORD:pwd}
  jpa:
    show-sql: true

logging:
  level:
    com.fbisquerra: DEBUG
